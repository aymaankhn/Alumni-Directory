import 'package:flutter/cupertino.dart';

class ProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // This page can be just for navigation
    // You can add content to this page if needed
    return Container();
  }
}
